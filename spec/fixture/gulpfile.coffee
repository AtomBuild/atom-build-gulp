'use strict'

gulp = require 'gulp'

gulp.task 'coffee-task-1', ->
  console.log 'this is derived from gulpfile.coffee'

gulp.task 'coffee-task-2', ->
  console.log 'this is derived from gulpfile.coffee'